apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: biteiq-https-redirect
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: biteiq-api-http-redirect
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: biteiq-biteiq-https-redirect@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: biteiq.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: biteiq-api
                port:
                  name: http

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: biteiq-api
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: biteiq-api
                port:
                  name: http
